{
  "name": "wellside",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=web",
    "build": "turbo build",
    "lint": "turbo lint",
    "start": "npm run start -w web"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
