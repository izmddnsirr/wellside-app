{
  "name": "wellside",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=web",
    "build": "turbo build",
    "lint": "turbo lint",
    "start": "npm run start -w web"
  },
  "devDependencies": {
    "turbo": "^2.8.17"
  }
}
